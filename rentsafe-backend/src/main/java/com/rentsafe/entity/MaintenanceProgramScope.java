package com.rentsafe.entity;

/**
 * Whether a preventive maintenance record applies to a single unit or an entire property (site-wide).
 */
public enum MaintenanceProgramScope {
    UNIT,
    PROPERTY
}
